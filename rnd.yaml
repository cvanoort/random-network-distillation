# replayer.py uses the exptag module, which is not present on PYPI.
#
name: rnd
channels:
dependencies:
  - python=3.7
  - pip
  - tensorflow-gpu=1.14
  - pip:
    - baselines
    - gym[atari]
    - ipdb
    - matplotlib
    - mpi4py
    - numpy
    - opencv-python
    - pandas
    - psutil
