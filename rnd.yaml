# replayer.py uses the exptag module, which is not present on PYPI.
#
name: rnd
channels:
dependencies:
  - python=3.7
  - pip
  - tensorflow-gpu=1.14
  - pip:
    - baselines
    - cv2
    - gym[atari]
    - ipdb
    - matplotlib
    - mpi4py
    - numpy
    - pandas
    - psutil
