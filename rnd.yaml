# replayer.py uses the exptag module, which is not present on PYPI.
#
name: rnd
channels:
dependencies:
  - python=3.7
  - pip
  - tensorflow-gpu=1.14
  - pip:
    - baselines
    - gym[atari]
    - ipdb
    - mpi4py
    - numpy
    - pandas
    - psutil
